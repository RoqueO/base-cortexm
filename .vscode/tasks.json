{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "CMake: build",
			"command": "cmake --build build-gcc --target build-all",
			// "targets": [
			// 	"all"
			// ],
			// "preset": "all-toolchains",
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": ["$gcc"],
			"detail": "Build using both toolchains",
			"presentation": {
                "reveal": "always",
                "panel": "new"
            }
		}
	]
}